# EmailJS configuration (create a file named `.env` in this folder for local dev)
# Get these values from https://dashboard.emailjs.com
# - `REACT_APP_EMAILJS_PUBLIC_KEY`: your EmailJS public key (recommended variable name)
#   Older examples sometimes use `REACT_APP_USER_ID`; this project will accept either.
# - `REACT_APP_SERVICE_ID`: the EmailJS service ID (e.g., 'service_xxx')
# - `REACT_APP_TEMPLATE_ID`: the EmailJS template ID (e.g., 'template_xxx')

# Example values (replace with your real keys):
# REACT_APP_EMAILJS_PUBLIC_KEY=your_public_key_here
# REACT_APP_SERVICE_ID=service_xxx
# REACT_APP_TEMPLATE_ID=template_xxx

# Save this file as `.env` in the `client/` directory (do NOT commit `.env` with secrets).
